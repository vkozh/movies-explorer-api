module.exports.formatUserData = ({ name, email }) => ({ name, email });

module.exports.formatMovieData = ({
  country,
  director,
  duration,
  year,
  description,
  image,
  trailerLink,
  nameRU,
  nameEN,
  thumbnail,
  movieId,
  owner,
  _id,
}) => ({
  country,
  director,
  duration,
  year,
  description,
  image,
  trailerLink,
  nameRU,
  nameEN,
  thumbnail,
  movieId,
  owner,
  _id,
});
