module.exports.formatUserData = ({ name, email }) => ({ name, email });

module.exports.formatMovieData = ({
  country,
  director,
  duration,
  year,
  description,
  image,
  trailer,
  nameRU,
  nameEN,
  thumbnail,
  movieId,
  owner,
}) => ({
  country,
  director,
  duration,
  year,
  description,
  image,
  trailer,
  nameRU,
  nameEN,
  thumbnail,
  movieId,
  owner,
});
